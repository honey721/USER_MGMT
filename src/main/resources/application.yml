spring:
  datasource:
    url: jdbc:mysql://mysql:3306/booksdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

app:
  jwt:
    # Keep secret in env var for production; this is fallback
    secret: replace-with-secure-secret
